// Side-effect kept intentionally: the pnpm migration pulled in newer webpack
// and terser versions that DCE pure `export default { … }` modules imported
// as bare specifiers (`import './test-es6-1'`). Without this assignment the
// module is dropped from vendors.js and the `transpileDependencies` assertion
// in project-basic.build.e2e.js has nothing to match against. Revisit once
// the build toolchain is upgraded so we can express the same coverage
// without leaning on a global.
global.__es6_1_loaded = true

export default {
  testToArray1 (...args) {
    return args
  }
}
